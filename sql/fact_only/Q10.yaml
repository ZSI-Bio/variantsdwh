---
queryId: Q10

queryType: SELECT

queryEngine: Hive

storageFormat: "{{DATA_FORMAT}}"

queryDesc: Q10

statement: "{{IF_EXPLAIN}}  SELECT COUNT (DISTINCT f_variant_name) FROM {{DB_NAME}}.fact_{{DATA_FORMAT}} where f_chr = 'chr20' GROUP BY f_sample_id LIMIT 10"

