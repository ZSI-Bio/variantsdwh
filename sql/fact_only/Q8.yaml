---
queryId: Q8

queryType: SELECT

queryEngine: Hive

storageFormat: "{{DATA_FORMAT}}"

queryDesc: Q8

statement: "{{IF_EXPLAIN}}  SELECT COUNT(DISTINCT f_variant_name) FROM {{DB_NAME}}.fact_{{DATA_FORMAT}} WHERE f_alter_depth/ (f_total_depth+0.00001) >0.9 AND f_sample_id =13685"

